import type { NextPage } from 'next';
import styles from './page.module.css';

const Home: NextPage = () => {
	return (
		<div className={styles.page}>
			<div>main page</div>
		</div>
	);
};

export default Home;
